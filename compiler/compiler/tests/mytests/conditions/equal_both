BEGIN
    IF 0 = 0 THEN
        WRITE 1;
    ENDIF

    IF 123 = 123 THEN
        WRITE 2;
    ENDIF

    IF 123 = 0 THEN
        WRITE 3;
    ENDIF

    IF 124 = 43 THEN
        WRITE 4;
    ENDIF

    IF 0 = 125 THEN
        WRITE 5;
    ENDIF

    IF 44 = 126 THEN
        WRITE 6;
    ENDIF
END