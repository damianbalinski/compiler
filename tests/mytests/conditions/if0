BEGIN
    WRITE 10;

    IF 2 < 3 THEN
        WRITE 1;
        WRITE 2;
        WRITE 3;
    ENDIF

    IF 4 < 3 THEN
        WRITE 10;
        WRITE 20;
        WRITE 30;
    ENDIF

    WRITE 30;
END